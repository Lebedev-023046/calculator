const processExpression = (event, dispatch) => {
  switch (event.target.textContent) {
    case '=':
      dispatch()
      break
    case 'CE':
      break
    case 'C':
      break

    default:
      break
  }
}
